A work in progress AmigaOS port of cairo with native surface and font
backends.

Currently only a static library is built, but a real shared library (not a
shared object) might be considered once the native backends are reasonably
finished and stable.

For a shared library version of pixman go to:

https://github.com/salass00/pixman_lib

